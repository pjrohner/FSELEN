package de.oio;

import java.util.concurrent.TimeUnit;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.firefox.FirefoxDriver;

public class HelloSelenium {
    private static final int EXPLICITLY_WAIT_TIME_MS = 5000;

    public static void main(String[] args) throws InterruptedException {
        // Load via system property or via PATH settings.

		// Go the URL under test.

		// Get the search field element.
		
		// Enter the search criteria.
		
		// Submit the search.

		// Find the searched content and select it by clicking on it.

		// Close the Firefox and the web driver.
    }
}
